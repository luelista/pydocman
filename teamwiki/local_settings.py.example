
# DROPME_STORE_DIRECTORY = '/var/lib/pydrop/store'

# THEWIKI_DATA_DIR = '/var/lib/wikidata'
# THEWIKI_PAGE_DIR = THEWIKI_DATA_DIR + '/pagedata'

# SECURITY WARNING: don't run with debug turned on in production!
# DEBUG = False
# ALLOWED_HOSTS = ["pydrop.example.org", ".wiki.example.org"]


# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'xxxxxxxxxxxxxx change this xxxxxxxxxxxxx'
PASSWORD_SALT = 'xxxxxxxxxxxxxx change this xxxxxxxxxxxxx'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'pydropdb',
        'USER': 'root',
        'PASSWORD': '',
        'HOST': 'localhost',
    }
}

